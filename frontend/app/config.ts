const WEB_TITLE = "TD4School";

const TD4_SETTINGS = {
  title: WEB_TITLE,
};

const WEB_PUBLIC_ADDRESS = "localhost"; // <= change here, ex: 192.168.1.10, example.com

const WEB_URL = `http://${WEB_PUBLIC_ADDRESS}`;

export default TD4_SETTINGS;
export { WEB_URL };
